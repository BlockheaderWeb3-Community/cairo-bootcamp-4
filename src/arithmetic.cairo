

pub mod logic {
    pub fn sub_operation(x: u16, y: u16) -> u16 {
        return x - y;
    }
    
    pub fn mul_operation(x: u256, y:u256) -> u256 {
        return x * y;
    }
    
    pub fn div_operation(x: u32, y: u32) -> u32 {
        return x / y;
    }
}